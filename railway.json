{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "PYTHONPATH=src uv run --no-dev uvicorn trade_view.main:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300
  }
}
